/*
Assignment: Write a function USING RECURSION that takes an integer input and returns true if the
number is even and false if the number is odd.
 */

var is_even = function (num) {
//    Your code here
};

// TESTING

if (!is_even(12))
    console.log('Fails on 12');
if (is_even(13))
    console.log('Fails on 13');
if (!is_even(0))
    console.log('Fails on 0');
if (is_even(1))
    console.log('Fails on 1');
if (is_even(-1))
    console.log(('Fails on -1'));